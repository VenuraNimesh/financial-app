services:
  postgres:
    image: postgres:17
    container_name: postgres-db-edu
    restart: unless-stopped

    environment:
      POSTGRES_DB: financial_edu
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password

    ports:
      - "5433:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d financial_edu"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data: